name: Experiment
on: 
  push:
    branches:
      - main

jobs:
  job1:
    name: Debug
    runs-on: ubuntu-latest

    steps:
    - name: Git checkout
      uses: actions/checkout@v2
      with: { ref: main }
    - name: Add files
      run: |
      mkdir ${{ github.event.repository.updated_at}}
      cd ${{ github.event.repository.updated_at}}
      echo "hello world" > trufflehog-anzx-debug.out
      cd ..
    - name: Commit back to GitHub
      run: |
        git config --global user.name "pulkitanz"
        git config --global user.email "pulkit.aggarwal@anz.com"
        git add -A
        git commit -m "Updating some file"
        git push

